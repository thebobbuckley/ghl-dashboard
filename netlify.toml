[build]
  functions = "netlify/functions"
  publish   = "public"

# Allow GHL (and any domain) to embed this app in an iframe
[[headers]]
  for = "/*"
    [headers.values]
    Content-Security-Policy = "frame-ancestors *"
    X-Frame-Options          = "ALLOWALL"
